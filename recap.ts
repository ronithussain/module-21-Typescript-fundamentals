const books: string = 'abd';
const runs:number[] = [33, 55, 66];

const shopping : [string, number] = ['phone', 15000];

const book : {name:'chemestry', author:string, price:number, pages:number, isUsed?:boolean} = {
    name: 'chemestry',
    author: 'mkoin',
    price: 500,
    pages: 33,
    // isUsed: true
}
// book.name = 'psysics'; // etake literls bola hoy